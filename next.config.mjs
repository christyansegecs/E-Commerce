/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'localhost',
            'https://e-commerce-production-ad3a.up.railway.app/'
        ]
    }
};

export default nextConfig;
