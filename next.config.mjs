/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'localhost',
            'digitalhippo-production.up.railway.app'
        ]
    }
};

export default nextConfig;
