/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'localhost',
            'e-commerce-production-ad3a.up.railway.app'
        ]
    }
};

export default nextConfig;
